# ==========================================
# KinexMed Rehabilitation Backend Configuration
# ==========================================

# Server Host & Port
HOST=0.0.0.0
PORT=8000

# Local LLM Endpoint (Optional)
# KinexMed falls back to deterministic rule-based grounded summaries if offline
LOCAL_LLM_URL=http://localhost:11434/api/generate

# Clinician Web Dashboard Origin
CORS_ORIGINS=["http://localhost:5173", "http://127.0.0.1:5173"]
